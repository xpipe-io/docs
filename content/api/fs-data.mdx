---
title: Store a raw blob to be used later
description: >
  Stores arbitrary binary data in a blob such that it can be used later on to
  for example write to a remote file.


  This will return a uuid which can be used as a reference to the blob.

  You can also store normal text data in blobs if you intend to create text or
  shell script files with it.
full: true
_openapi:
  method: POST
  route: /fs/blob
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          Stores arbitrary binary data in a blob such that it can be used later
          on to for example write to a remote file.


          This will return a uuid which can be used as a reference to the blob.

          You can also store normal text data in blobs if you intend to create
          text or shell script files with it.
---

<APIPage document={"./public/openapi.yaml"} operations={[{"path":"/fs/blob","method":"post"}]} webhooks={[]} hasHead={false} />