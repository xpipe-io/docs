---
title: Stop shell connection
description: >
  Stops an existing shell session for a connection.


  This operation will return once the shell has exited.

  If the shell is busy or stuck, you might have to work with timeouts to account
  for these cases.
full: true
_openapi:
  method: POST
  route: /shell/stop
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          Stops an existing shell session for a connection.


          This operation will return once the shell has exited.

          If the shell is busy or stuck, you might have to work with timeouts to
          account for these cases.
---

<APIPage document={"./public/openapi.yaml"} operations={[{"path":"/shell/stop","method":"post"}]} webhooks={[]} hasHead={false} />