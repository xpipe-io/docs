---
title: Python API
description: Interacting with XPipe from Python
---

## Introduction

The XPipe Python API is a Python client for the XPipe HTTP API. It is essentially a wrapper for the raw [HTTP API](/api) and intended to make working with it more convenient. If you are not that comfortable with Python, the API can also be used in other languages via a simple HTTP client library that supports exchanging json requests.

You can find the source code at https://github.com/xpipe-io/xpipe-python-api.

The primary use cases for the Python API are:

- Automatic import and migration of lots of servers from other sources where configuring them manually it would take too much time, e.g. if you want to migrate ~1000 servers to XPipe. There are only limited migration tools for other tools, so using the API makes sense, especially when it comes to also importing secrets.
- Automated management of a fleet of servers. For example, upgrading or configuring a tool on all servers. Especially when the workflow is more than just running a command, e.g., different control flows based on the server configuration. Furthermore, this allows for an automated aggregation of results to check whether the task completed successfully on all systems.

## Installation

```bash
python3 -m pip install xpipe_api
```

## Connecting to the XPipe application

To start out, you need to enable the API access in the XPipe settings. You can find that under **Settings** -> **HTTP API**. For only local API access, you don't need to worry about the API key in the settings menu.

```python
from xpipe_api import Client

# By default, Client() will read an access key from the file xpipe_auth on the local filesystem
# and talk to the XPipe HTTP server on localhost.
client = Client()

# If you want to connect to a PTB build of XPipe, you have to configure the client with that information
# as the PTB releases use a different port
client = Client(ptb=True)

# To connect to a remote instance with an API key
# To make an XPipe instance allow external connections, i.e. not on localhost,
# you have to enable the "Allow external API requests" setting in the API settings menu
client = Client(token="foo", base_url = "http://servername:21721")
```

There's also an async version of the client that can be accessed as AsyncClient. All calls are run asynchronously with that client. This page will only cover the sync client though.

```python
import asyncio
from xpipe_api import AsyncClient

async def main():
    client = AsyncClient()

    # Do your stuff async

if __name__ == "__main__":
    asyncio.run(main())
```

## Stores and connections

In the context of the API, XPipe refers to any kind of connection, identity, script, and others as a `store`.
This just refers to the fact that those entries store json configuration data and some metadata.

## Querying connections

A store reference is just a UUID string. You can query one or multiple existing stores that are stored in XPipe based on various filters.

[**API reference**](/api/store-query)

```python
from xpipe_api import Client

client = Client()

# store_query accepts glob-based filters on the category, store name, and store type

# Find all stores in the default category and all its subcategories
stores = client.store_query(categories="Default/**")

# Find all stores in the default category without any subcategories
stores = client.store_query(categories="Default/*")

# Find all top level stores in the default category
stores = client.store_query(categories="Default", stores="*")

# Find all stores in the default category, including any sub stores
stores = client.store_query(categories="Default", stores="**")

# Find all SSH config stores on the local machine
stores = client.store_query(categories="Default", stores="Local Machine/SSH user config/*")

# Find only ssh config host stores in the "MyCategory" category that has the word "MyCompany" in its name
# - Searching for strings is case-insensitive here
# - This will not include subcategories of "MyCategory"
# - You can find all the available store type names by just querying all stores and taking a look add the individual type names of the stores.
stores = client.store_query(categories="MyCategory", types="sshConfigHost", stores="*MyCompany*")

# Query a specific store by name in a category
# This will only return one element, so we can just access that
store = client.store_query(categories="MyCategory", stores="MyConnectionName")[0]

# Query the local machine connection
store = client.store_query(stores="Local Machine")[0]

# A store reference is just a UUID string, so you can also specify it fixed
store = "f0ec68aa-63f5-405c-b178-9a4454556d6b"
```

You can obtain the UUID of a store in the GUI as well via the context menu. If the API is enabled in the settings menu, you will find a new option in the context menu for stores and categories:

![](/images/api-copy-uuid.png)

## Getting store information

Since each store reference is just a UUID string, you have to retrieve information for it using another API call.

[**API reference**](/api/store-info)

```python
from xpipe_api import Client

client = Client()

# Returns an array of UUID strings
stores = client.store_query(categories="Default/**")

# Works in bulk, so it expects an array of stores
infos = client.store_info(stores)
# Get the first info
info = infos[0]

# The store UUID
uuid = info["store"]
# This is an array containing all category hierarchy names
category = info["category"]
# This is an array containing all store hierarchy names
name = info["name"]
# This is the store type name that you can use in the query
type = info["type"]
# There is also some other data for internal state management, e.g. if a tunnel is running for example
```

## Adding stores

You can add custom stores as well.

[**API reference**](/api/store-add)

The schema for each store type is not publicly documented as it might be subject to change. However, you can see the current active schemas for every store and use that as a basis.

```python
from xpipe_api import Client

client = Client()

# The store reference to the store you want to expect the schema of
# If you have the HTTP API setting enabled in XPipe, you can copy the API UUID of a store in its context menu
store = "f0ec68aa-63f5-405c-b178-9a4454556d6b"

# The schema is just a json object
schema = client.store_info(stores)[0]["config"]
```

For an SSH connection, it could, for example, look something like this if it references an existing identity:

```json
{
  "type" : "ssh",
  "gateway" : "f0ec68aa-63f5-405c-b178-9a4454556d6b",
  "host" : "myip.eu-central-1.compute.amazonaws.com",
  "port" : 22,
  "identity" : {
    "type" : "ref",
    "ref" : "34c7ea9a-b00d-45e1-a324-78ef0750acc2"
  },
  "dontInteractWithSystem" : false,
  "forwardX11" : false,
  "jumpServer" : false,
  "additionalOptions" : null
}
```

or something like this if it has encrypted credentials configured in-place:

```json
{
  "type" : "ssh",
  "gateway" : "f0ec68aa-63f5-405c-b178-9a4454556d6b",
  "host" : "myip.eu-central-1.compute.amazonaws.com",
  "port" : 22,
  "identity" : {
    "type" : "inPlace",
    "identityStore" : {
      "type" : "localIdentity",
      "username" : "User",
      "password" : {
        "secrets" : [ {
          "name" : "vault",
          "principal" : "be815152-05d2-4094-84d3-f0eea9200d5f",
          "iteration" : 1,
          "secret" : "qJu3y-hwyo1Q63Btf68My6zOV9RL_wom9k6UjlFcIU9OmhLy6RTJxEwtq9x7O5h-tjCzKdWpN99VFU80QoFzsTx_NqXniZh3SS1fT_LMFQ5Feh8tYH9BIy9OvBFvuWTwRF7YmoWIAv8NCcpNLeROGtGHW0X6VuJQkBKiLQhJavHaiJNnORGj9Ud0cv1uDubQWgRtcYdYsFJlrsxr6naUVXTm9i9knUeTDjgrPg==",
          "token" : "YneQNpGM33zWvPUMoUaf9u_eDIyhH6PZDJ1PfFfEfgR1deQiVXTbVQBdzthtODZqd4JNQTl3jCp5rcZ2EhvfSg=="
        } ]
      },
      "sshIdentity" : {
        "secrets" : [ {
          "name" : "vault",
          "principal" : "be815152-05d2-4094-84d3-f0eea9200d5f",
          "iteration" : 1,
          "secret" : "vAWh6XbCT5-Z0O7kLfYkAZTelm5KSPwrG5sMAgGsjgRRxxtaXxcW0wWRePKunYvHo7ag",
          "token" : "V2XrIJH9GFhgyRFbn_rBcMz45c-lnwLHzL0AQ7XT0I5AfOM60zK_92xZHnbXgmk2tyBC79WaYazMFlxIsHg28w=="
        } ]
      }
    }
  },
  "dontInteractWithSystem" : false,
  "forwardX11" : false,
  "jumpServer" : false,
  "additionalOptions" : null
}
```

When adding connections with credentials, it is preferred to create the identity within XPipe and then just reference it from the API. However, you can still create identities fully manually if you want, it just takes a bit more work as you have to encrypt the secrets first.

### Encrypting secrets

The challenge when adding identities via the API is encrypting secrets so that they can be stored correctly:

```python
encrypted_secret = client.secret_encrypt("passw0rd")
# {"secrets":[{"name":"vault","principal":"be815152-05d2-4094-84d3-f0eea9200d5f","iteration":1,"secret":"WJKXh5cZCq33JOIRn8cfKcs8nJCRT7rU_3t7UfASGwbCp1k8","token":"ixTTpdpjZpEVfYEXOnUbgO4a7I008tPG-tTq2jg45VMT1iEyXoFB05IsaMuAx8OdiRZ--cjtFsw1cK-IuhkIHQ=="}]}
print(encrypted_secret)

decrypted_secret = client.secret_decrypt(encrypted_secret)
# passw0rd
print(decrypted_secret)
```

Note that you can't decrypt the secrets of these samples yourself if you try, as your XPipe instance uses another unique encryption key. If you're also using a custom vault user passphrase or have configured a team vault, then the encryption key is unique to your user and roles as well.

### Encrypting password auth information

When specifying information about any kind of password authentication, you will have to encrypt more than just a raw password. As XPipe supports many different ways of retrieving secrets for password authentication, you will have to include this information as well.

```python
# No password is used at all
raw_password_auth = {
    'type' : 'none'
}

encrypted_password_auth = client.secret_encrypt(json.dumps(raw_password_auth))
# {'secret': {'type': 'locked', 'encryptedValue': '0N_LSNW_Gu_kz-HzBmNC5VcMY7kJuIN-Dam0bv370fSTgIJyGPENklzovp_9kCdvPS206m-giPr-fVfV6Q=='}, 'encryptedToken': {'token': 'bDTjRHsCMgvwPiXPEt5xF6JbLuj5UuaBSheF75pB5A=='}}
print(encrypted_password_auth)
```

There are also other types of schemas for password auth available. For example, to use a specific in-place password, you would use the following schema and encrypt it:

```python
# Password is used
raw_password_auth = {
  'type': 'inPlace',
  'value': client.secret_encrypt("passw0rd")
}

# {'secret': {'type': 'locked', 'encryptedValue': 'UhC-BIzAKx2NVNsUajnibzbctPEAIysJ9FqEcYTDG0ur-XroO-21RLUxgyxItkk3BqP8YsMLFUHYYVaEb5IsCbTD_A-M_9zj4lGBYVkFwLpdyLWXGp6m9CaNDoJv5-m4-DTzr_eS9HsDNb_hHmfAG9Vc8I4dSc6HdLbk4zF4DKvFgqN9JfljzHzmPDSnBafDIV9ZRbtKavOZ0ZvH2Ph7jO5Jr64cqX0GiPEXlU1IA95koiViLOjC2hXx4xxCxn8zvq3mHYjZLMKiGzrQqwGfAppwGE1hZbS0em9OUErG8mLKvhJCQkwEFYqJxw=='}, 'encryptedToken': {'token': 'Ifnc2k1nxdF2ZPQcn8zq6engoYi3nm1iKZ1UF3SDlg=='}}
encrypted_password_auth = client.secret_encrypt(json.dumps(raw_password_auth))
```

### Encrypting key information

For SSH, any identities like key references also need to be encrypted. XPipe itself doesn't store the private keys itself, it will only keep references like a file path. These are, however, still encrypted.

With that, you can now create store schemas and add them like this:

```python
# No SSH key is used at all
raw_key = '''{
    "type" : "none"
}'''

encrypted_key = client.secret_encrypt(raw_key)
# {'secret': {'type': 'locked', 'encryptedValue': '0N_LSNW_Gu_kz-HzBmNC5VcMY7kJuIN-Dam0bv370fSTgIJyGPENklzovp_9kCdvPS206m-giPr-fVfV6Q=='}, 'encryptedToken': {'token': 'bDTjRHsCMgvwPiXPEt5xF6JbLuj5UuaBSheF75pB5A=='}}
print(encrypted_key)
```

There are also other types of schemas for keys available. For example, to use a specific key file, you would use the following schema and encrypt it:

```python
# SSH key file is used
raw_key = '''{
  "type" : "file",
  "file" : "C:/Users/User/.ssh/key.pem",
  "password" : {
    "type" : "none"
  }
}'''

encrypted_key = client.secret_encrypt(raw_key)
```

Of course, there are also more variants for SSH keys. To inspect the key information of an existing store, you can use the `secret_decrypt` function.

### Putting it together

Now that the password and key information have been encrypted, you can add a new connection with credentials:

```python
from xpipe_api import Client

client = Client()

# The connection schema
data = {
    "type": "ssh",
    "gateway": "f0ec68aa-63f5-405c-b178-9a4454556d6b",
    "host" : "myip.eu-central-1.compute.amazonaws.com",
    "port" : 22,
    "identity" : {
        "type" : "inPlace",
        "identityStore" : {
            "type" : "localIdentity",
            "username" : "User",
            "password" : encrypted_password_auth,
            "sshIdentity" : encrypted_key
        }
    },
    "dontInteractWithSystem" : False,
    "forwardX11" : False,
    "jumpServer" : False,
    "additionalOptions" : None
}

# The target category to put this connection into
category = "97458c07-75c0-4f9d-a06e-92d8cdf67c40"

# The validate option controls whether XPipe should verify that a connection can be established to the system
client.store_add(name="My Connection", conn_data=data, validate=True, category=category)
```

### Using reusable identities

To skip the need to pass secrets in-place, you can also create identities instead. Once you have created an identity in XPipe, you can then just use the identity UUID as a reference:

```python
from xpipe_api import Client

client = Client()

# The connection schema
data = {
    "type": "ssh",
    "gateway": "f0ec68aa-63f5-405c-b178-9a4454556d6b",
    "host" : "myip.eu-central-1.compute.amazonaws.com",
    "port" : 22,
    "identity" : {
        "type" : "ref",
        "ref" : "34c7ea9a-b00d-45e1-a324-78ef0750acc2"
    },
    "dontInteractWithSystem" : False,
    "forwardX11" : False,
    "jumpServer" : False,
    "additionalOptions" : None
}
```

This will make the store creation much easier. You can also create identities through the same API and with the same encrypted values for the password and SSH key; however, it is easier to use in the GUI.

### Adding other stores

While the process of adding an SSH connection has been quite complex due to the encryption, other ways aren't that complex.

To start off, you can also skip adding any data you don't want to include and instead leave it empty. This will still add the connection, but will keep it in an invalid state where you have to configure any missing data in the XPipe interface if you click on it. If you are looking to just import the hostnames first without all the secrets, assigning `"identity": None` also works.

Furthermore, most store types, for example shell environments, do not require any secrets and can therefore be created much easier:

```python
data = {
    "type": "shellEnvironment",
    "commands": "echo test",
    "host": "f0ec68aa-63f5-405c-b178-9a4454556d6b",
    "shell": "pwsh",
    "elevated": False,
    "user": None,
    "scripts": None
}
```

### Controlling the access scope

You can also control the access scope of a secret by passing a custom principal. A principal is a vault user or role that has access to the secret. You can pass this principal to the `/secret/encrypt` endpoint to control who has access to this secret. If you don't pass a principal, the secret is available to the whole vault. If you pass a principal, e.g. `my-user` for a personal vault or `["role1", "role2"]` for a team vault, then only this selection has access to the secret:

```python
encrypted_secret = client.secret_encrypt("passw0rd", ["role1", "role2"])
# {"secrets":[{"name":"role1","principal":"7bf51507-0a58-4703-869d-765a781c0b94","iteration":1,"secret":"oE87dJArBFPSnT98EujbRwf0TNjpHCoyvKNdyxi0KPLzbbTL","token":"5pVhGtEBOGJ9e8m807AWb12hcet4of1930TNKP4GJ1v7PkoudDbyAUSyykU8pnTXJzAr_a3ruzNPyfnj9jL8Ow=="},{"name":"role2","principal":"3209a888-be37-4037-b8b0-b14adda69ea2","iteration":1,"secret":"g-1vinHvLm0KSzKx9RtXOTuTgbn_ubyLilxPFDpiCjlk1TIp","token":"aHTc4ZbuL07WBeDjYttk5IbGYif4n5Zm3tTOQ6ap1B07jKUEchoPbyroD6hnqUDrgLX7cHxBc1T_grkpVjbHFw=="}]}
print(encrypted_secret)
```

When creating synced identities via the API, you will also have to explicitly set the `accessScope` property along with encrypting the secrets with the correct principals. Instead of the principal names like `role1`, the store data uses the principal UUIDs to prevent issues when renaming them. You can find the the principal UUIDs in the `user.json` or `roles.json` file within your synced storage git repo.

Assuming that you want to make this synced identity accessible to `role1` and `role2`, and have already encrypted the password and key data with the `/secret/encrypt` endpoint and the right principals, you can then create the store with:

```
{
  "type" : "sycnedIdentity",
  "username" : "User",
  "password" : <encrypted password json>,
  "sshIdentity" : <encrypted key json>
  "accessScope" : [ "95061f85-59dd-46cd-8dbd-73f8f9a07800", "5d3dc174-62d9-40d2-81cb-2ca66c6bb1d1" ]
}
```

The secret system is cryptographically secure, and the identity access scope is only visual. On the UI level, the `accessScope` value is used to determine whether a user has access to a store. If the `accessScope` and actual secret encryption principals do not match, the user will get decryption errors if they don't have access to the underlying secrets. So the actually security-relevant principals are those for the encrypted secrets.

## Actions

You can perform actions for the desktop application from the API as well. For more information on actions in general, see the [actions](/guide/actions) page.

[**API reference**](/api/action)

```python
from xpipe_api import Client

client = Client()

# Opens a terminal session for a connection
client.action(json.loads('''{
  "id" : "open"
  "ref" : "f0ec68aa-63f5-405c-b178-9a4454556d6b"
}'''), confirm=False)

# Toggles the session state for a store. If this store is a tunnel, then this operation will start or stop the tunnel
client.action(json.loads('''{
  "id" : "toggleStore",
  "ref" : "4dc0c7d4-083e-446e-8a75-7a29490eaec1",
  "enabled" : true,
}'''), confirm=False)

# Run a file browser action like chmod
client.action(json.loads('''{
  "id" : "chmod",
  "ref" : "1aa86bc3-9ab8-4b2e-b3bc-dd028d142faf",
  "files" : [ "/home/ubuntu/archive.tar.gz" ],
  "permissions" : "755",
  "recursive" : false
}'''), confirm=False)

# The confirm option gives you a GUI dialog to first confirm
# the action with the parameters before executing it to prevent any mistakes
```

## Shell operations

You can open remote shell sessions to systems and run arbitrary commands in them.

```python
from xpipe_api import Client
import re

client = Client()

store = "f0ec68aa-63f5-405c-b178-9a4454556d6b"

# This will start a shell session for the connection
# Note that this session is non-interactive, meaning that password prompts are not supported
shell_info = client.shell_start(store)

# The shell dialect of the shell. For example cmd, powershell, bash, etc.
dialect = shell_info["shellDialect"]
# The operating system type of the system. For example, windows, linux, macos, bsd, solaris
osType = shell_info["osType"]
# The display name of the operating system. For example Windows 10 Home 22H2
osName = shell_info["osName"]

# Prints {'exitCode': 0, 'stdout': 'hello world', 'stderr': ''}
print(client.shell_exec(store, "echo hello world"))

# Prints {'exitCode': 0, 'stdout': '<user>', 'stderr': ''}
print(client.shell_exec(store, "echo $USER"))

# Prints {'exitCode': 127, 'stdout': 'invalid: command not found', 'stderr': ''}
print(client.shell_exec(store, "invalid"))

# Extract ssh version from system
version_format = re.compile(r'[0-9.]+')
ret = client.shell_exec(store, "ssh -V")
ssh_version = version_format.findall(ret["stderr"])[0]

# Clean up after ourselves by stopping the shell session
client.shell_stop(store)
```

## File system operations

You can interact with the file system of any remote shell as well.

```python
from xpipe_api import Client

client = Client()

store = "f0ec68aa-63f5-405c-b178-9a4454556d6b"

# We need a shell for the file system
client.shell_start(store)

# You are responsible for decoding files in the correct file encoding
file_bytes = client.fs_read(store, "~/.ssh/config")
file_string = file_bytes.decode('utf-8')

# Writing files can be done via blobs
file_write_content = "test\nfile"
blob_id = client.fs_blob(file_write_content)
client.fs_write(store, blob_id, "~/test_file.txt")

# You can do the same with script files as well
# This will create an executable script in the system's temp directory
script_write_content = "echo hello\necho world"
blob_script_id = client.fs_blob(file_write_content)
script_path = client.fs_script(store, blob_script_id)

# You can then use this script for your commands
# Prints {'exitCode': 0, 'stdout': 'hello\nworld', 'stderr': ''}
print(client.shell_exec(store, "\"" + script_path + "\""))

# Clean up after ourselves by stopping the shell session
client.shell_stop(store)
```
